# 如何添加新语言支持

> **版本**: 1.0  
> **更新日期**: 2025-11-03  
> **适用对象**: 技术人员、系统管理员

---

## 📋 概述

本指南说明如何为CMS分类系统添加新的语言支持。以**添加日语**为例。

**难度**：⭐ 简单  
**预计时间**：15分钟  
**需要重启**：✅ 是（重启Django服务）  
**需要迁移**：❌ 否（无需数据库迁移）

---

## 🎯 添加日语支持（示例）

### 步骤1：修改Django配置

#### 文件：`core/settings.py`

找到`PARLER_LANGUAGES`配置（约第543行），添加日语：

**修改前**：
```python
PARLER_LANGUAGES = {
    None: (
        {'code': 'zh-hans', 'name': 'Simplified Chinese'},  # 简体中文
        {'code': 'en', 'name': 'English'},                  # 英文
        {'code': 'zh-hant', 'name': 'Traditional Chinese'}, # 繁体中文
    ),
    'default': {
        'fallbacks': ['zh-hans'],
        'hide_untranslated': False,
    }
}
```

**修改后**：
```python
PARLER_LANGUAGES = {
    None: (
        {'code': 'zh-hans', 'name': 'Simplified Chinese'},  # 简体中文
        {'code': 'en', 'name': 'English'},                  # 英文
        {'code': 'zh-hant', 'name': 'Traditional Chinese'}, # 繁体中文
        {'code': 'ja', 'name': 'Japanese'},                 # 日语 🆕
    ),
    'default': {
        'fallbacks': ['zh-hans'],
        'hide_untranslated': False,
    }
}
```

### 步骤2：更新Admin显示配置

#### 文件：`cms/admin.py`

**位置1：translation_status方法**（约第149行）

添加日语到`lang_map`：

```python
lang_map = {
    'zh-hans': ('简', '#4CAF50'),
    'en': ('EN', '#2196F3'),
    'zh-hant': ('繁', '#FF9800'),
    'ja': ('日', '#F44336')  # 🆕 添加日语，红色标识
}
```

**位置2：show_all_translations方法**（约第187行）

添加日语到`lang_names`：

```python
lang_names = {
    'zh-hans': '简体中文',
    'en': 'English',
    'zh-hant': '繁体中文',
    'ja': '日本語'  # 🆕 添加日语
}
```

### 步骤3：更新序列化器

#### 文件：`cms/serializers.py`

找到`CategorySerializer.to_representation`方法（约第43行）：

**修改前**：
```python
if current_language not in ['zh-hans', 'en', 'zh-hant']:
    current_language = 'zh-hans'
```

**修改后**：
```python
if current_language not in ['zh-hans', 'en', 'zh-hant', 'ja']:  # 🆕 添加'ja'
    current_language = 'zh-hans'
```

### 步骤4：重启Django服务

```bash
# 开发环境
# 按Ctrl+C停止服务，然后重新运行
python manage.py runserver

# 生产环境
sudo systemctl restart gunicorn
# 或
sudo supervisorctl restart your_app
```

### 步骤5：测试

#### Admin后台测试

1. 访问：`http://localhost:8000/admin/cms/category/`
2. 编辑任意分类
3. 应该能看到：**简体中文 | English | 繁体中文 | 日本語**
4. 点击"日本語"标签
5. 填写日语翻译并保存

#### API测试

```bash
# 测试获取日语分类
curl -X GET 'http://localhost:8000/api/v1/cms/categories/' \
  -H 'X-Tenant-ID: 1' \
  -H 'Accept-Language: ja'

# 预期：name等字段返回日语内容（如果有翻译）
# 如果没有日语翻译，会回退到默认语言（简体中文）
```

---

## 📝 添加其他语言

### 常用语言代码对照表

| 语言 | 代码 | Name | 示例短标识 | 建议颜色 |
|------|------|------|-----------|---------|
| 简体中文 | zh-hans | Simplified Chinese | 简 | #4CAF50 绿色 |
| 繁体中文 | zh-hant | Traditional Chinese | 繁 | #FF9800 橙色 |
| 英语 | en | English | EN | #2196F3 蓝色 |
| 日语 | ja | Japanese | 日 | #F44336 红色 |
| 韩语 | ko | Korean | 한 | #9C27B0 紫色 |
| 法语 | fr | French | FR | #3F51B5 靛蓝 |
| 德语 | de | German | DE | #607D8B 灰蓝 |
| 西班牙语 | es | Spanish | ES | #FF5722 深橙 |
| 俄语 | ru | Russian | РУ | #E91E63 粉红 |
| 阿拉伯语 | ar | Arabic | AR | #009688 青色 |
| 泰语 | th | Thai | ไท | #CDDC39 黄绿 |
| 越南语 | vi | Vietnamese | VI | #FFC107 琥珀 |

### 添加韩语示例

```python
# settings.py
PARLER_LANGUAGES = {
    None: (
        {'code': 'zh-hans', 'name': 'Simplified Chinese'},
        {'code': 'en', 'name': 'English'},
        {'code': 'zh-hant', 'name': 'Traditional Chinese'},
        {'code': 'ja', 'name': 'Japanese'},
        {'code': 'ko', 'name': 'Korean'},  # 🆕 韩语
    ),
    ...
}

# admin.py - translation_status
lang_map = {
    'zh-hans': ('简', '#4CAF50'),
    'en': ('EN', '#2196F3'),
    'zh-hant': ('繁', '#FF9800'),
    'ja': ('日', '#F44336'),
    'ko': ('한', '#9C27B0')  # 🆕 韩语
}

# admin.py - show_all_translations
lang_names = {
    'zh-hans': '简体中文',
    'en': 'English',
    'zh-hant': '繁体中文',
    'ja': '日本語',
    'ko': '한국어'  # 🆕 韩语
}

# serializers.py
if current_language not in ['zh-hans', 'en', 'zh-hant', 'ja', 'ko']:  # 🆕 添加'ko'
    current_language = 'zh-hans'
```

---

## ❓ 常见问题

### Q1: 添加新语言需要数据库迁移吗？

**A**: ❌ **不需要**！

翻译表（cms_category_translation）已经包含language_code字段，可以存储任何语言代码。

### Q2: 添加新语言后，现有分类会自动有翻译吗？

**A**: ❌ **不会**。

新语言只是添加了支持，需要在Admin后台手动为每个分类添加该语言的翻译。

### Q3: 如果只添加了配置但没添加翻译会怎样？

**A**: 系统会使用**回退语言**（简体中文）的内容。

**示例**：
```bash
# 请求日语，但分类没有日语翻译
curl -H "Accept-Language: ja" /api/v1/cms/categories/1/

# 返回：
{
  "name": "技术",  # 返回简体中文（回退语言）
  "translations": {
    "zh-hans": {"name": "技术"},
    # 没有ja的翻译
  }
}
```

### Q4: 可以同时添加多个语言吗？

**A**: ✅ **可以**！

在`PARLER_LANGUAGES`中一次性添加多个语言即可：

```python
PARLER_LANGUAGES = {
    None: (
        {'code': 'zh-hans', 'name': 'Simplified Chinese'},
        {'code': 'en', 'name': 'English'},
        {'code': 'zh-hant', 'name': 'Traditional Chinese'},
        {'code': 'ja', 'name': 'Japanese'},     # 日语
        {'code': 'ko', 'name': 'Korean'},       # 韩语
        {'code': 'fr', 'name': 'French'},       # 法语
        {'code': 'de', 'name': 'German'},       # 德语
    ),
    ...
}
```

### Q5: 可以删除某个语言吗？

**A**: ⚠️ **谨慎操作**！

**删除步骤**：
1. 从`PARLER_LANGUAGES`配置中移除该语言
2. （可选）从数据库删除该语言的翻译记录
3. 重启服务

**注意**：
- 删除后，该语言的翻译数据仍在数据库中
- 前端请求该语言会回退到默认语言
- 如需彻底删除，需要手动清理数据库

```sql
-- 删除所有日语翻译（谨慎！）
DELETE FROM cms_category_translation WHERE language_code = 'ja';
```

### Q6: 如何修改默认语言？

**A**: 修改`PARLER_DEFAULT_LANGUAGE_CODE`：

```python
# 如果要改为英文作为默认语言
PARLER_DEFAULT_LANGUAGE_CODE = 'en'

# 同时建议修改fallbacks
'fallbacks': ['en', 'zh-hans'],
```

---

## 🔄 完整的添加新语言流程

### 以添加法语为例

#### 1. 修改配置（3处）

**settings.py**：
```python
{'code': 'fr', 'name': 'French'},  # 法语
```

**admin.py - translation_status**：
```python
'fr': ('FR', '#3F51B5')  # 法语，靛蓝色
```

**admin.py - show_all_translations**：
```python
'fr': 'Français'  # 法语
```

**serializers.py**：
```python
if current_language not in ['zh-hans', 'en', 'zh-hant', 'ja', 'fr']:
    current_language = 'zh-hans'
```

#### 2. 重启服务

```bash
python manage.py runserver  # 开发环境
# 或
sudo systemctl restart gunicorn  # 生产环境
```

#### 3. 在Admin添加翻译

1. 登录Admin后台
2. 编辑分类
3. 点击"Français"标签（自动出现）
4. 填写法语翻译
5. 保存

#### 4. 测试API

```bash
curl -H "Accept-Language: fr" \
     -H "X-Tenant-ID: 1" \
     http://localhost:8000/api/v1/cms/categories/
```

#### 5. 更新前端

**Web前端**：
```javascript
const languages = [
  { code: 'zh-hans', label: '简体中文' },
  { code: 'en', label: 'English' },
  { code: 'zh-hant', label: '繁體中文' },
  { code: 'ja', label: '日本語' },
  { code: 'fr', label: 'Français' }  // 🆕
];
```

**iOS前端**：
```swift
enum Language: String {
    case zhHans = "zh-hans"
    case en = "en"
    case zhHant = "zh-hant"
    case ja = "ja"
    case fr = "fr"  // 🆕
    
    var displayName: String {
        switch self {
        case .zhHans: return "简体中文"
        case .en: return "English"
        case .zhHant: return "繁體中文"
        case .ja: return "日本語"
        case .fr: return "Français"  // 🆕
        }
    }
}
```

#### 6. 更新文档

更新以下文档中所有提到支持语言的地方：
- 分类管理API.md
- CMS分类多语言使用指南.md
- Django_Admin分类翻译管理手册.md
- 前端CMS集成快速指南.md
- README.md

---

## 📊 语言支持矩阵

### 当前支持的语言（v3.0+）

| 语言 | 代码 | Admin标识 | 颜色 | 状态 |
|------|------|----------|------|------|
| 简体中文 | zh-hans | 简 | 🟢 绿色 | ✅ 已支持（默认） |
| 英文 | en | EN | 🔵 蓝色 | ✅ 已支持 |
| 繁体中文 | zh-hant | 繁 | 🟠 橙色 | ✅ 已支持 |
| 日语 | ja | 日 | 🔴 红色 | ✅ 已支持 |
| 韩语 | ko | 한 | 🟣 紫色 | ✅ 已支持 |
| 法语 | fr | FR | 🔷 靛蓝 | ✅ 已支持 |

### 推荐添加的语言

根据业务需求，可以考虑添加：

| 优先级 | 语言 | 代码 | 使用人群 |
|-------|------|------|---------|
| 高 | 英语 | en | 已支持 ✅ |
| 高 | 日语 | ja | 已支持 ✅ |
| 中 | 韩语 | ko | 东亚市场 |
| 中 | 西班牙语 | es | 拉美、欧洲 |
| 中 | 法语 | fr | 欧洲、非洲 |
| 中 | 德语 | de | 欧洲市场 |
| 低 | 俄语 | ru | 东欧市场 |
| 低 | 阿拉伯语 | ar | 中东市场 |

---

## 🎨 颜色选择建议

为新语言选择合适的颜色，确保：
1. **区分度高** - 不同语言颜色差异明显
2. **可读性好** - 白色文字在背景色上清晰可见
3. **符合习惯** - 可以使用国旗颜色等联想

**Material Design颜色推荐**：
```
绿色系：#4CAF50, #8BC34A, #009688
蓝色系：#2196F3, #03A9F4, #00BCD4
橙色系：#FF9800, #FF5722, #FFC107
紫色系：#9C27B0, #673AB7, #3F51B5
红色系：#F44336, #E91E63
灰色系：#607D8B, #9E9E9E
```

**已使用颜色**：
- 🟢 #4CAF50 (绿色) - 简体中文
- 🔵 #2196F3 (蓝色) - 英文
- 🟠 #FF9800 (橙色) - 繁体中文
- 🔴 #F44336 (红色) - 日语

**可用颜色**：
- #9C27B0 (紫色) - 推荐韩语
- #3F51B5 (靛蓝) - 推荐法语
- #607D8B (灰蓝) - 推荐德语
- #009688 (青色) - 推荐阿拉伯语

---

## ⚠️ 注意事项

### 1. 语言代码标准

**使用ISO 639-1代码**（2字母）或ISO 639-3（语言-地区）：

✅ **推荐**：
- `en` (英语)
- `ja` (日语)
- `zh-hans` (简体中文)
- `zh-hant` (繁体中文)

❌ **避免**：
- `cn` (中国不是语言)
- `chinese` (使用zh)
- `english` (使用en)

### 2. 从右到左（RTL）语言

如添加阿拉伯语、希伯来语等RTL语言，需要额外配置：

```python
# settings.py
PARLER_LANGUAGES = {
    None: (
        {'code': 'ar', 'name': 'Arabic'},
    ),
    'default': {
        'fallbacks': ['zh-hans'],
        'hide_untranslated': False,
    }
}

# 前端需要添加RTL支持
```

### 3. 性能考虑

- 每添加一种语言，翻译表数据量增加
- 建议只添加真正需要的语言
- 太多语言会增加管理复杂度

### 4. 翻译维护

- 每添加一种语言，需要翻译所有分类
- 建议：
  - 先添加语言配置
  - 逐步添加重要分类的翻译
  - 而不是一次性翻译所有

---

## 🔄 回退语言配置

### 单一回退语言（当前配置）

```python
'fallbacks': ['zh-hans'],  # 所有语言都回退到简体中文
```

### 多级回退

```python
# 示例：日语 → 英语 → 简体中文
'fallbacks': ['en', 'zh-hans'],
```

### 针对特定语言的回退

```python
PARLER_LANGUAGES = {
    None: (
        {'code': 'zh-hans'},
        {'code': 'en'},
        {'code': 'ja'},
        {'code': 'ko'},
    ),
    'default': {
        'fallbacks': ['en', 'zh-hans'],  # 默认回退顺序
    },
    1: {  # 租户1的特殊配置
        'fallbacks': ['zh-hans', 'en'],  # 中文优先
    }
}
```

---

## 📋 添加语言检查清单

在添加新语言前，使用此清单确保完整：

### 配置检查

- [ ] settings.py - PARLER_LANGUAGES中添加
- [ ] admin.py - translation_status的lang_map中添加
- [ ] admin.py - show_all_translations的lang_names中添加
- [ ] serializers.py - 语言代码检查列表中添加

### 测试检查

- [ ] Django服务已重启
- [ ] Admin后台能看到新语言标签
- [ ] 可以在Admin中添加新语言翻译
- [ ] 翻译状态显示新语言标识
- [ ] API请求新语言能正常响应

### 前端检查

- [ ] 语言选择器UI已更新
- [ ] 语言列表配置已添加
- [ ] 测试语言切换功能

### 文档检查

- [ ] API文档已更新
- [ ] 使用指南已更新
- [ ] Admin手册已更新
- [ ] README已更新

---

## 🚀 快速命令

### 一键添加日语（所有命令汇总）

```bash
# 1. 修改配置文件（手动）
# 编辑 core/settings.py, cms/admin.py, cms/serializers.py

# 2. 重启服务
python manage.py runserver

# 3. 测试API
curl -H "Accept-Language: ja" -H "X-Tenant-ID: 1" \
  http://localhost:8000/api/v1/cms/categories/

# 4. 访问Admin测试
open http://localhost:8000/admin/cms/category/
```

---

## 📚 相关文档

- [分类管理API.md](./分类管理API.md) - API接口文档
- [CMS分类多语言使用指南.md](./CMS分类多语言使用指南.md) - 使用指南
- [Django_Admin分类翻译管理手册.md](./Django_Admin分类翻译管理手册.md) - Admin操作

---

## 💡 最佳实践

### 1. 渐进式添加

```
阶段1: 先添加配置
  ↓
阶段2: 为重要分类添加翻译
  ↓
阶段3: 逐步完善所有分类
  ↓
阶段4: 前端集成新语言
```

### 2. 翻译优先级

**高优先级**：
- 首页显示的分类
- 导航菜单中的分类
- 热门分类

**中优先级**：
- 次要导航分类
- 专题分类

**低优先级**：
- 归档分类
- 很少使用的分类

### 3. 翻译质量保证

- ✅ 使用专业翻译人员
- ✅ 人工校对机器翻译结果
- ✅ 保持术语一致性
- ✅ 符合目标语言习惯
- ✅ 测试实际显示效果

---

**添加新语言就是这么简单！** 🌐✨

