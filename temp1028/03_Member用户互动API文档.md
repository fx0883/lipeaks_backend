# Member用户互动 API 文档

> **适用对象**: 前端开发人员（iOS/Web/Android）  
> **Base URL**: `/api/v1/interactions/`  
> **文档版本**: 2.0  
> **最后更新**: 2025-10-31

---

## 📋 目录

1. [功能概述](#功能概述)
2. [点赞功能](#点赞功能)
3. [关注功能](#关注功能)
4. [收藏功能](#收藏功能)
5. [集成流程](#集成流程)
6. [数据类型定义](#数据类型定义)
7. [常见问题](#常见问题)

---

## 功能概述

Member用户互动功能包括三大模块：

### 1. 点赞功能 👍
Member用户可以给其他Member用户点赞，表示认可和支持。

**核心功能**:
- 点赞其他用户
- 查看我点赞的用户列表
- 查看收到的点赞列表
- 检查点赞状态
- 取消点赞

### 2. 关注功能 ➕
Member用户可以关注其他Member用户，建立关注关系。

**核心功能**:
- 关注其他用户
- 查看关注列表（我关注的人）
- 查看粉丝列表（关注我的人）
- 查看互相关注列表（好友）
- 获取关注统计数据
- 检查关注状态
- 取消关注

### 3. 收藏功能 ⭐
用户可以收藏文章，方便后续查看。

**核心功能**:
- 收藏文章
- 查看收藏列表
- 检查收藏状态
- 取消收藏

### 业务规则

✅ **允许的操作**:
- 点赞/关注本租户内的其他Member用户
- 收藏本租户内的文章
- 管理自己的互动记录

❌ **禁止的操作**:
- 点赞/关注自己
- 跨租户操作
- 重复点赞/关注/收藏

---

## 点赞功能

### 1.1 获取我点赞的用户列表

#### 接口信息
- **URL**: `/api/v1/interactions/likes/`
- **方法**: `GET`
- **权限**: Member用户

#### 请求参数

| 参数名 | 类型 | 位置 | 必填 | 说明 | 默认值 |
|--------|------|------|------|------|--------|
| page | integer | query | 否 | 页码 | 1 |
| page_size | integer | query | 否 | 每页数量 | 10 |

#### 响应格式

**成功响应 (200)**:
```json
{
  "count": 10,
  "next": "https://your-domain.com/api/v1/interactions/likes/?page=2",
  "previous": null,
  "results": [
    {
      "id": 15,
      "from_member": 5,
      "to_member": 8,
      "from_member_info": {
        "id": 5,
        "username": "member_user",
        "nick_name": "普通用户",
        "avatar": "/media/avatars/avatar1.jpg"
      },
      "to_member_info": {
        "id": 8,
        "username": "another_member",
        "nick_name": "另一个用户",
        "avatar": "/media/avatars/avatar2.jpg"
      },
      "tenant": 1,
      "created_at": "2025-10-20T10:30:00Z"
    }
  ]
}
```

---

### 1.2 点赞用户

#### 接口信息
- **URL**: `/api/v1/interactions/likes/`
- **方法**: `POST`
- **权限**: Member用户
- **Content-Type**: `application/json`

#### 请求参数

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| to_member | integer | 是 | 被点赞用户的ID |

#### 请求示例

```http
POST /api/v1/interactions/likes/ HTTP/1.1
Host: your-domain.com
Authorization: Bearer eyJ0eXAiOiJKV1QiLCJhbGc...
X-Tenant-ID: 1
Content-Type: application/json

{
  "to_member": 8
}
```

#### 响应格式

**成功响应 (201)**: 返回点赞记录详情（格式同列表响应）

**错误响应 (400)**:
```json
{
  "to_member": ["您已经点赞过该用户"]
}
```

或

```json
{
  "to_member": ["不能点赞自己"]
}
```

---

### 1.3 取消点赞

#### 接口信息
- **URL**: `/api/v1/interactions/likes/{id}/`
- **方法**: `DELETE`
- **权限**: Member用户，只能删除自己的点赞

#### 路径参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | integer | 是 | 点赞记录ID |

#### 响应格式

**成功响应 (204)**: 无响应体

---

### 1.4 获取收到的点赞列表

#### 接口信息
- **URL**: `/api/v1/interactions/likes/received/`
- **方法**: `GET`
- **权限**: Member用户

#### 请求参数

支持分页参数（page、page_size）

#### 响应格式

**成功响应 (200)**: 返回点赞列表（格式同"获取我点赞的用户列表"）

---

### 1.5 通过用户ID取消点赞

#### 接口信息
- **URL**: `/api/v1/interactions/likes/by-member/{member_id}/`
- **方法**: `DELETE`
- **权限**: Member用户

#### 路径参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| member_id | integer | 是 | 被点赞用户ID |

#### 响应格式

**成功响应 (204)**:
```json
{
  "message": "已取消点赞"
}
```

**使用场景**: 在用户详情页，已知用户ID但不知道点赞记录ID时使用

---

### 1.6 检查是否已点赞用户

#### 接口信息
- **URL**: `/api/v1/interactions/likes/check/{member_id}/`
- **方法**: `GET`
- **权限**: Member用户

#### 路径参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| member_id | integer | 是 | 用户ID |

#### 响应格式

**已点赞 (200)**:
```json
{
  "is_liked": true,
  "like_id": 15,
  "created_at": "2025-10-20T10:30:00Z"
}
```

**未点赞 (200)**:
```json
{
  "is_liked": false,
  "like_id": null,
  "created_at": null
}
```

**使用场景**: 在显示用户卡片/详情时，判断是否显示"已点赞"状态

---

## 关注功能

### 2.1 获取我的关注列表

#### 接口信息
- **URL**: `/api/v1/interactions/follows/`
- **方法**: `GET`
- **权限**: Member用户

#### 请求参数

支持分页参数（page、page_size）

#### 响应格式

**成功响应 (200)**:
```json
{
  "count": 15,
  "next": null,
  "previous": null,
  "results": [
    {
      "id": 30,
      "follower": 5,
      "following": 12,
      "follower_info": {
        "id": 5,
        "username": "member_user",
        "nick_name": "普通用户",
        "avatar": "/media/avatars/avatar1.jpg"
      },
      "following_info": {
        "id": 12,
        "username": "followed_user",
        "nick_name": "被关注用户",
        "avatar": "/media/avatars/avatar4.jpg"
      },
      "is_mutual": true,
      "tenant": 1,
      "created_at": "2025-10-20T11:00:00Z"
    }
  ]
}
```

#### 响应字段说明

| 字段名 | 类型 | 说明 |
|--------|------|------|
| is_mutual | boolean | 是否互相关注（重要字段） |
| follower_info | object | 关注者信息 |
| following_info | object | 被关注者信息 |

---

### 2.2 关注用户

#### 接口信息
- **URL**: `/api/v1/interactions/follows/`
- **方法**: `POST`
- **权限**: Member用户
- **Content-Type**: `application/json`

#### 请求参数

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| following | integer | 是 | 要关注的用户ID |

#### 请求示例

```http
POST /api/v1/interactions/follows/ HTTP/1.1
Host: your-domain.com
Authorization: Bearer eyJ0eXAiOiJKV1QiLCJhbGc...
X-Tenant-ID: 1
Content-Type: application/json

{
  "following": 12
}
```

#### 响应格式

**成功响应 (201)**: 返回关注记录（包含is_mutual字段）

**错误响应 (400)**:
```json
{
  "following": ["您已经关注过该用户"]
}
```

---

### 2.3 取消关注

#### 接口信息
- **URL**: `/api/v1/interactions/follows/{id}/`
- **方法**: `DELETE`
- **权限**: Member用户

#### 路径参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | integer | 是 | 关注记录ID |

#### 响应格式

**成功响应 (204)**: 无响应体

---

### 2.4 获取粉丝列表

#### 接口信息
- **URL**: `/api/v1/interactions/follows/followers/`
- **方法**: `GET`
- **权限**: Member用户

#### 响应格式

**成功响应 (200)**: 返回粉丝列表（格式同关注列表）

**说明**: 
- 粉丝列表显示的是关注我的用户
- 每条记录中`follower`是粉丝，`following`是我

---

### 2.5 通过用户ID取消关注

#### 接口信息
- **URL**: `/api/v1/interactions/follows/by-member/{member_id}/`
- **方法**: `DELETE`
- **权限**: Member用户

#### 路径参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| member_id | integer | 是 | 被关注用户ID |

#### 响应格式

**成功响应 (204)**:
```json
{
  "message": "已取消关注"
}
```

---

### 2.6 检查是否已关注用户

#### 接口信息
- **URL**: `/api/v1/interactions/follows/check/{member_id}/`
- **方法**: `GET`
- **权限**: Member用户

#### 路径参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| member_id | integer | 是 | 用户ID |

#### 响应格式

**已关注 (200)**:
```json
{
  "is_following": true,
  "follow_id": 30,
  "is_mutual": true,
  "created_at": "2025-10-20T11:00:00Z"
}
```

**未关注 (200)**:
```json
{
  "is_following": false,
  "follow_id": null,
  "is_mutual": false,
  "created_at": null
}
```

**字段说明**:
- `is_mutual`: 是否互相关注（重要：可用于显示"好友"状态）

---

### 2.7 获取互相关注列表

#### 接口信息
- **URL**: `/api/v1/interactions/follows/mutual/`
- **方法**: `GET`
- **权限**: Member用户

#### 响应格式

**成功响应 (200)**: 返回互相关注的用户列表

**说明**: 
- 互相关注 = 我关注了对方 且 对方也关注了我
- 可以理解为"好友列表"
- 所有记录的`is_mutual`字段都是true

---

### 2.8 获取关注统计信息

#### 接口信息
- **URL**: `/api/v1/interactions/follows/stats/`
- **方法**: `GET`
- **权限**: Member用户

#### 响应格式

**成功响应 (200)**:
```json
{
  "following_count": 15,
  "followers_count": 25,
  "mutual_count": 10
}
```

**字段说明**:

| 字段名 | 类型 | 说明 |
|--------|------|------|
| following_count | integer | 我关注了多少人 |
| followers_count | integer | 多少人关注我（粉丝数） |
| mutual_count | integer | 互相关注数（好友数） |

**使用场景**: 用户个人主页显示统计数据

---

## 收藏功能

### 3.1 获取我的收藏列表

#### 接口信息
- **URL**: `/api/v1/interactions/favorites/`
- **方法**: `GET`
- **权限**: 需要认证（Member和User都可用）

#### 请求参数

支持分页参数（page、page_size）

#### 响应格式

**成功响应 (200)**:
```json
{
  "count": 15,
  "next": "https://your-domain.com/api/v1/interactions/favorites/?page=2",
  "previous": null,
  "results": [
    {
      "id": 23,
      "user": 5,
      "article": 42,
      "article_detail": {
        "id": 42,
        "title": "深入理解Python装饰器",
        "slug": "python-decorators",
        "excerpt": "本文详细介绍Python装饰器的原理和应用...",
        "cover_image": "https://example.com/python.jpg",
        "author_info": {
          "id": 3,
          "username": "author"
        },
        "status": "published",
        "views_count": 1250
      },
      "user_info": {
        "id": 5,
        "username": "member_user"
      },
      "tenant": 1,
      "created_at": "2025-10-20T10:30:00Z"
    }
  ]
}
```

---

### 3.2 收藏文章

#### 接口信息
- **URL**: `/api/v1/interactions/favorites/`
- **方法**: `POST`
- **权限**: 需要认证
- **Content-Type**: `application/json`

#### 请求参数

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| article | integer | 是 | 要收藏的文章ID |

#### 请求示例

```http
POST /api/v1/interactions/favorites/ HTTP/1.1
Host: your-domain.com
Authorization: Bearer eyJ0eXAiOiJKV1QiLCJhbGc...
X-Tenant-ID: 1
Content-Type: application/json

{
  "article": 42
}
```

#### 响应格式

**成功响应 (201)**: 返回收藏记录

**错误响应 (400)**:
```json
{
  "article": ["您已经收藏过这篇文章"]
}
```

---

### 3.3 取消收藏

#### 接口信息
- **URL**: `/api/v1/interactions/favorites/{id}/`
- **方法**: `DELETE`
- **权限**: 需要认证，只能删除自己的收藏

#### 路径参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| id | integer | 是 | 收藏记录ID |

#### 响应格式

**成功响应 (204)**: 无响应体

---

### 3.4 通过文章ID取消收藏

#### 接口信息
- **URL**: `/api/v1/interactions/favorites/by-article/{article_id}/`
- **方法**: `DELETE`
- **权限**: 需要认证

#### 路径参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| article_id | integer | 是 | 文章ID |

#### 响应格式

**成功响应 (204)**:
```json
{
  "message": "已取消收藏"
}
```

**使用场景**: 在文章详情页，已知文章ID但不知道收藏记录ID

---

### 3.5 检查文章是否已收藏

#### 接口信息
- **URL**: `/api/v1/interactions/favorites/check/{article_id}/`
- **方法**: `GET`
- **权限**: 需要认证

#### 路径参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| article_id | integer | 是 | 文章ID |

#### 响应格式

**已收藏 (200)**:
```json
{
  "is_favorited": true,
  "favorite_id": 23,
  "created_at": "2025-10-20T10:30:00Z"
}
```

**未收藏 (200)**:
```json
{
  "is_favorited": false,
  "favorite_id": null,
  "created_at": null
}
```

---

## 集成流程

### 点赞功能集成流程

```
┌──────────────────┐
│  显示用户信息     │
│  (用户卡片/详情) │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  调用检查点赞API │
│  GET /likes/     │
│  check/{id}/     │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  根据is_liked    │
│  显示按钮状态     │
│  - 已点赞: 高亮  │
│  - 未点赞: 正常  │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  用户点击按钮     │
└────────┬─────────┘
         │
    ┌────┴────┐
    │  判断   │
    └────┬────┘
         │
    ┌────┴────┐
    │         │
    ▼         ▼
┌────────┐ ┌────────┐
│ 已点赞 │ │ 未点赞 │
└───┬────┘ └───┬────┘
    │          │
    ▼          ▼
┌────────┐ ┌────────┐
│ DELETE │ │ POST   │
│ 取消   │ │ 点赞   │
└───┬────┘ └───┬────┘
    │          │
    └────┬─────┘
         │
         ▼
┌──────────────────┐
│  更新UI状态      │
└──────────────────┘
```

### 关注功能集成流程

```
┌──────────────────┐
│  显示用户信息     │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  调用检查关注API │
│  GET /follows/   │
│  check/{id}/     │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  显示按钮状态     │
│  - 未关注: 关注  │
│  - 已关注: 已关注│
│  - 互关: 互相关注│
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  用户点击按钮     │
└────────┬─────────┘
         │
    ┌────┴────┐
    │  判断   │
    └────┬────┘
         │
    ┌────┴────┐
    │         │
    ▼         ▼
┌────────┐ ┌────────┐
│ 已关注 │ │ 未关注 │
│ 取消   │ │ 关注   │
└───┬────┘ └───┬────┘
    │          │
    ▼          ▼
┌────────┐ ┌────────┐
│ DELETE │ │ POST   │
└───┬────┘ └───┬────┘
    │          │
    └────┬─────┘
         │
         ▼
┌──────────────────┐
│  检查is_mutual   │
│  更新按钮样式     │
└──────────────────┘
```

### 收藏功能集成流程

```
┌──────────────────┐
│  显示文章详情     │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  调用检查收藏API │
│  GET /favorites/ │
│  check/{id}/     │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  显示收藏按钮     │
│  已收藏: ❤️ 填充 │
│  未收藏: ♡ 描边  │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  用户点击         │
└────────┬─────────┘
         │
    ┌────┴────┐
    │         │
    ▼         ▼
┌────────┐ ┌────────┐
│ 已收藏 │ │ 未收藏 │
│ 取消   │ │ 收藏   │
└───┬────┘ └───┬────┘
    │          │
    └────┬─────┘
         │
         ▼
┌──────────────────┐
│  更新按钮状态     │
└──────────────────┘
```

### 个人主页统计显示流程

```
┌──────────────────┐
│  加载个人主页     │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  调用统计API     │
│  GET /follows/   │
│  stats/          │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  显示统计数据     │
│  - 关注: 15      │
│  - 粉丝: 25      │
│  - 好友: 10      │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  点击统计项       │
│  跳转对应列表页   │
└──────────────────┘
```

---

## 数据类型定义

### 用户信息对象

```
UserInfo {
  id: Integer
  username: String
  nick_name: String
  avatar: String
}
```

### 点赞记录对象

```
MemberLike {
  id: Integer
  from_member: Integer
  to_member: Integer
  from_member_info: UserInfo
  to_member_info: UserInfo
  tenant: Integer
  created_at: String (ISO 8601 DateTime)
}
```

### 关注记录对象

```
MemberFollow {
  id: Integer
  follower: Integer
  following: Integer
  follower_info: UserInfo
  following_info: UserInfo
  is_mutual: Boolean
  tenant: Integer
  created_at: String (ISO 8601 DateTime)
}
```

### 收藏记录对象

```
ArticleFavorite {
  id: Integer
  user: Integer
  article: Integer
  article_detail: {
    id: Integer
    title: String
    slug: String
    excerpt: String
    cover_image: String
    author_info: {
      id: Integer
      username: String
    }
    status: String
    views_count: Integer
  }
  user_info: {
    id: Integer
    username: String
  }
  tenant: Integer
  created_at: String (ISO 8601 DateTime)
}
```

### 检查状态响应对象

**点赞状态**:
```
LikeCheckResponse {
  is_liked: Boolean
  like_id: Integer | null
  created_at: String | null
}
```

**关注状态**:
```
FollowCheckResponse {
  is_following: Boolean
  follow_id: Integer | null
  is_mutual: Boolean
  created_at: String | null
}
```

**收藏状态**:
```
FavoriteCheckResponse {
  is_favorited: Boolean
  favorite_id: Integer | null
  created_at: String | null
}
```

---

## 使用场景说明

### 场景1: 用户列表页（显示互动按钮）

**需要的API**:
1. 获取用户列表（其他API）
2. 对每个用户调用检查点赞API
3. 对每个用户调用检查关注API

**优化建议**:
- 考虑缓存检查结果（5分钟）
- 只在用户视口内的卡片才检查状态
- 实现懒加载

---

### 场景2: 用户详情页（完整互动功能）

**需要的API**:
1. `GET /likes/check/{member_id}/` - 检查点赞
2. `GET /follows/check/{member_id}/` - 检查关注
3. `POST /likes/` - 点赞
4. `POST /follows/` - 关注
5. `DELETE /likes/by-member/{member_id}/` - 取消点赞
6. `DELETE /follows/by-member/{member_id}/` - 取消关注

**显示逻辑**:
- 点赞按钮：已点赞显示高亮
- 关注按钮：
  - 未关注：显示"关注"
  - 已关注：显示"已关注"
  - 互相关注：显示"互相关注"（特殊样式）

---

### 场景3: 我的关注页面（Tab切换）

**Tab1 - 我的关注**: `GET /follows/`  
**Tab2 - 我的粉丝**: `GET /follows/followers/`  
**Tab3 - 互相关注**: `GET /follows/mutual/`

**功能**:
- Tab切换调用不同API
- 显示用户列表
- 每个用户显示互动按钮
- 支持取消关注操作

---

### 场景4: 个人主页（统计展示）

**需要的API**:
1. `GET /follows/stats/` - 获取统计数据

**显示内容**:
```
┌─────────────────────┐
│  个人主页            │
├─────────────────────┤
│  头像  用户名        │
│  ─────────────      │
│  关注  粉丝  好友    │
│   15    25    10    │
│  (可点击跳转列表)    │
└─────────────────────┘
```

**交互**:
- 点击"关注"数字 → 跳转关注列表
- 点击"粉丝"数字 → 跳转粉丝列表
- 点击"好友"数字 → 跳转互相关注列表

---

### 场景5: 文章详情页（收藏按钮）

**需要的API**:
1. `GET /favorites/check/{article_id}/` - 检查收藏状态
2. `POST /favorites/` - 收藏文章
3. `DELETE /favorites/by-article/{article_id}/` - 取消收藏

**按钮状态**:
- 未收藏：♡ 空心图标
- 已收藏：❤️ 实心图标（红色）

---

## 集成要点

### 1. 乐观更新建议

**什么是乐观更新**:
在API请求发送前，立即更新UI状态，给用户即时反馈。如果请求失败，再回滚状态。

**适用场景**:
- ✅ 点赞/取消点赞
- ✅ 关注/取消关注
- ✅ 收藏/取消收藏

**实现步骤**:
1. 用户点击按钮
2. 立即更新UI状态（按钮变色/文字改变）
3. 发送API请求
4. 如果成功：保持UI状态
5. 如果失败：回滚UI状态 + 显示错误提示

---

### 2. 状态缓存建议

**缓存策略**:
- 点赞/关注状态：缓存1-3分钟
- 统计数据：缓存5分钟
- 列表数据：不缓存（实时性要求高）

**缓存位置**:
- iOS: `NSCache`或内存
- Android: 内存或`SharedPreferences`
- Web: 内存或`localStorage`

**缓存刷新**:
- 用户主动刷新
- 执行操作后刷新相关缓存
- 超时自动失效

---

### 3. 批量状态检查

**问题**: 在用户列表页，如何高效获取多个用户的点赞/关注状态？

**方案A - 逐个请求**（当前）:
- 对每个用户调用check接口
- 优点：简单直接
- 缺点：请求数多

**方案B - 客户端缓存**（推荐）:
- 维护一个已点赞/已关注的ID集合
- 新用户才调用check接口
- 优点：减少请求
- 缺点：需要维护缓存

**方案C - 后端批量接口**（未来）:
- 一次请求获取多个用户的状态
- 优点：性能最优
- 缺点：需要后端支持

---

### 4. 互相关注识别

**is_mutual字段的使用**:

```
if (is_mutual == true) {
    // 显示特殊样式，如：
    // - 金色边框
    // - "互相关注"徽章
    // - 不同的按钮文字
}
```

**建议UI**:
- 普通关注：绿色按钮 "✓ 已关注"
- 互相关注：金色/紫色按钮 "✓ 互相关注"
- 可添加好友标识图标

---

## 常见问题

### Q1: 点赞和关注的区别是什么？
**A**: 
- **点赞**：轻量级的认可，类似"赞"
- **关注**：建立持续关系，可以看到对方动态（如果实现了动态功能）

### Q2: 互相关注是什么？
**A**: 当两个用户互相关注时，他们成为"好友"关系，`is_mutual`字段为true。

### Q3: 可以点赞/关注其他租户的用户吗？
**A**: 不可以。所有互动都限制在同一租户内，跨租户操作会返回403错误。

### Q4: 如何实现"一键互关"功能？
**A**: 
1. 检查对方是否关注了我（通过粉丝列表或check接口）
2. 如果对方已关注我，显示"回关"按钮
3. 点击后调用关注API
4. 关注成功后，`is_mutual`会变为true

### Q5: 收藏功能只有Member可以用吗？
**A**: 不是。收藏功能支持所有认证用户（User和Member都可以）。

### Q6: 如何显示"收到的点赞"数量？
**A**: 调用`GET /likes/received/`接口，响应中的`count`字段就是总数。

### Q7: 统计数据是实时的吗？
**A**: 是的，每次调用都会实时计算。建议客户端缓存5分钟。

---

## 错误处理

### 点赞功能错误

| 错误场景 | 状态码 | 错误信息 |
|---------|--------|---------|
| 重复点赞 | 400 | `{"to_member": ["您已经点赞过该用户"]}` |
| 点赞自己 | 400 | `{"to_member": ["不能点赞自己"]}` |
| 跨租户点赞 | 400 | `{"to_member": ["您无法点赞其他租户的用户"]}` |
| 非Member用户 | 403 | `{"to_member": ["只有Member用户可以点赞"]}` |
| 用户不存在 | 404 | `{"detail": "未找到"}` |

### 关注功能错误

| 错误场景 | 状态码 | 错误信息 |
|---------|--------|---------|
| 重复关注 | 400 | `{"following": ["您已经关注过该用户"]}` |
| 关注自己 | 400 | `{"following": ["不能关注自己"]}` |
| 跨租户关注 | 400 | `{"following": ["您无法关注其他租户的用户"]}` |
| 非Member用户 | 403 | `{"following": ["只有Member用户可以关注"]}` |

### 收藏功能错误

| 错误场景 | 状态码 | 错误信息 |
|---------|--------|---------|
| 重复收藏 | 400 | `{"article": ["您已经收藏过这篇文章"]}` |
| 跨租户收藏 | 400 | `{"article": ["您无法收藏其他租户的文章"]}` |
| 文章不存在 | 400 | `{"article": ["Invalid pk..."]}` |

---

## API端点总览

### 点赞相关 (6个)

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/likes/` | 我点赞的用户 |
| POST | `/likes/` | 点赞用户 |
| DELETE | `/likes/{id}/` | 取消点赞 |
| GET | `/likes/received/` | 收到的点赞 |
| DELETE | `/likes/by-member/{member_id}/` | 通过ID取消点赞 |
| GET | `/likes/check/{member_id}/` | 检查点赞状态 |

### 关注相关 (8个)

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/follows/` | 我的关注 |
| POST | `/follows/` | 关注用户 |
| DELETE | `/follows/{id}/` | 取消关注 |
| GET | `/follows/followers/` | 我的粉丝 |
| DELETE | `/follows/by-member/{member_id}/` | 通过ID取消关注 |
| GET | `/follows/check/{member_id}/` | 检查关注状态 |
| GET | `/follows/mutual/` | 互相关注 |
| GET | `/follows/stats/` | 关注统计 |

### 收藏相关 (5个)

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/favorites/` | 收藏列表 |
| POST | `/favorites/` | 收藏文章 |
| DELETE | `/favorites/{id}/` | 取消收藏 |
| DELETE | `/favorites/by-article/{article_id}/` | 通过ID取消收藏 |
| GET | `/favorites/check/{article_id}/` | 检查收藏状态 |

---

## 性能优化建议

### 1. 减少API调用
- ✅ 缓存点赞/关注状态
- ✅ 使用check接口而不是获取完整列表来判断状态
- ✅ 批量操作时考虑后端支持

### 2. 提升用户体验
- ✅ 乐观更新（立即UI反馈）
- ✅ 显示加载状态
- ✅ 平滑的动画过渡

### 3. 数据加载
- ✅ 使用分页避免一次加载过多数据
- ✅ 实现无限滚动或懒加载
- ✅ 统计数据定期刷新而非实时

---

## 测试检查清单

### 点赞功能
- [ ] 成功点赞用户
- [ ] 成功取消点赞
- [ ] 重复点赞被拒绝
- [ ] 点赞自己被拒绝
- [ ] 查看点赞列表正常
- [ ] 查看收到的点赞正常
- [ ] 检查点赞状态准确

### 关注功能
- [ ] 成功关注用户
- [ ] 成功取消关注
- [ ] 重复关注被拒绝
- [ ] 关注自己被拒绝
- [ ] 查看关注列表正常
- [ ] 查看粉丝列表正常
- [ ] 互相关注列表正确
- [ ] 统计数据准确

### 收藏功能
- [ ] 成功收藏文章
- [ ] 成功取消收藏
- [ ] 重复收藏被拒绝
- [ ] 查看收藏列表正常
- [ ] 检查收藏状态准确

---

## 更新日志

| 版本 | 日期 | 说明 |
|------|------|------|
| 2.0 | 2025-10-31 | 移除前端代码，改为通用集成说明和流程图 |
| 1.0 | 2025-10-31 | 初始版本 |

---

**技术支持**: 如有问题，请联系后端团队。
